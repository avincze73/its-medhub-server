package tdsclients.radClient.imaging;

import java.awt.Point;
import java.awt.geom.Rectangle2D;

public class DRect2D extends Rectangle2D.Double{
//	public double x,y;
	public DRect2D(double x, double y, double w, double h)	{	super(x,y,w,h);	}
	
/*	public boolean contains(double checkX, double checkY){
		return super.contains(checkX, checkY);
	}*/
	
//	public double distanceFrom(double pX, double pY){	return Point.distance(x, y, pX, pY);	}
	
//	public Rectangle2D outerRounded(){	return new Point((int)Math.round(x), (int)Math.round(y));	}
//	public Rectangle2D innerRounded(){	return new Point((int)Math.round(x), (int)Math.round(y));	}
	
	
}
